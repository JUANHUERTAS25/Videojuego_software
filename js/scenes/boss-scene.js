// Escena del jefe final (esqueleto de migracion)
(function (global) {
    global.BossScene = {
        update: function () {
            // Aqui va la logica especifica del jefe final.
        },

        render: function () {
            // Aqui va el render de la escena del jefe final.
        }
    };
})(window);
