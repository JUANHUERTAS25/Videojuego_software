// Escena de juego normal (esqueleto de migracion)
(function (global) {
    global.PlayScene = {
        update: function () {
            // Aqui va la logica del juego normal.
        },

        render: function () {
            // Aqui va el render del juego normal.
        }
    };
})(window);
